apiVersion: v1
kind: Service
metadata:
    name: yieldguard-service
    namespace: yieldguard
spec:
    selector:
        app: yieldguard
    ports:
        - protocol: TCP
          port: 8000
          targetPort: 8000
    type: NodePort